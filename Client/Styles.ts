// Styles.tsx
import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    app: {
        backgroundColor: '#001',
        width: '100%'
    },
    button:{
        bottom: 0,
        position: 'absolute',
    }
});

export default styles;